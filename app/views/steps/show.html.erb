<h1>GeekTwitter</h1>
<h3>Tweet詳細</h3>
<div class="tweet">
  <p><%= @step.body %></p>
  <p><%= @step.created_at %></p>
</div>

<%= link_to "編集する", edit_step_path(@study.id) %> <%= link_to
"Tweet一覧に戻る", steps_path %>